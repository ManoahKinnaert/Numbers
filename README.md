# Numbers
This repo contains implementations of algorithms to convert decimal numbers to binary, to octal and to hexadecimal.
And this in mulitple programming languages. Not only that, but we will try to avoid built-in functions as much as possible (that is built-in functions to make such conversions).
